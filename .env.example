DISCORD_TOKEN=
DISCORD_APPLICATION_ID=
DISCORD_CLIENT_ID=
DISCORD_CLIENT_SECRET=

WEB_HOST=127.0.0.1
WEB_PORT=3000
BASE_URL=http://localhost:3000
OAUTH_REDIRECT_URI=http://localhost:3000/auth/callback
SESSION_SECRET=change-me-in-production-to-something-secure

DATABASE_URL=sqlite:data/db.sqlite
DATABASE_MAX_CONNECTIONS=10

RUST_LOG=clouder=info,tower_http=debug

EMBED_DIRECTORY="embed_files"
# Hours after which embed files are cleaned up (0 disables cleanup)
EMBED_MAX_AGE_HOURS=24
# How often to run cleanup (0 disables automatic cleanup)
EMBED_CLEANUP_INTERVAL_HOURS=6
